### Server side of GLCamp
## How to run
### Installation
- `npm run npm_install` - merging `client` and `server` dependencies
### Dev mode:
- `npm run dev`
### Prod mode:
- `npm run build`
- `npm run start`